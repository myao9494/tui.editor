import forEachArray_1 from '../collection/forEachArray.js';
import inArray_1 from '../array/inArray.js';
import { g as getClass_1 } from '../../common/getClass-077404ec.js';
import { _ as _setClassName } from '../../common/_setClassName-cee5adc0.js';
import '../type/isArray.js';
import '../type/isUndefined.js';

/**
 * Remove css class from element
 * @param {(HTMLElement|SVGElement)} element - target element
 * @param {...string} cssClass - css classes to remove
 * @memberof module:domUtil
 */
function removeClass(element) {
  var cssClass = Array.prototype.slice.call(arguments, 1);
  var classList = element.classList;
  var origin, newClass;

  if (classList) {
    forEachArray_1(cssClass, function(name) {
      classList.remove(name);
    });

    return;
  }

  origin = getClass_1(element).split(/\s+/);
  newClass = [];
  forEachArray_1(origin, function(name) {
    if (inArray_1(name, cssClass) < 0) {
      newClass.push(name);
    }
  });

  _setClassName(element, newClass);
}

var removeClass_1 = removeClass;

export { removeClass_1 as default };
