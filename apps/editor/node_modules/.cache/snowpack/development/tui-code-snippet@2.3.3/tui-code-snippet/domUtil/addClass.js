import { f as forEach_1 } from '../../common/forEach-bd9b35a9.js';
import inArray_1 from '../array/inArray.js';
import { g as getClass_1 } from '../../common/getClass-077404ec.js';
import { _ as _setClassName } from '../../common/_setClassName-cee5adc0.js';
import '../type/isArray.js';
import '../collection/forEachArray.js';
import '../collection/forEachOwnProperties.js';
import '../type/isUndefined.js';

/**
 * domUtil module
 * @module domUtil
 */

/**
 * Add css class to element
 * @param {(HTMLElement|SVGElement)} element - target element
 * @param {...string} cssClass - css classes to add
 * @memberof module:domUtil
 */
function addClass(element) {
  var cssClass = Array.prototype.slice.call(arguments, 1);
  var classList = element.classList;
  var newClass = [];
  var origin;

  if (classList) {
    forEach_1(cssClass, function(name) {
      element.classList.add(name);
    });

    return;
  }

  origin = getClass_1(element);

  if (origin) {
    cssClass = [].concat(origin.split(/\s+/), cssClass);
  }

  forEach_1(cssClass, function(cls) {
    if (inArray_1(cls, newClass) < 0) {
      newClass.push(cls);
    }
  });

  _setClassName(element, newClass);
}

var addClass_1 = addClass;

export { addClass_1 as default };
